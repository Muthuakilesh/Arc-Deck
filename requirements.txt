flask==3.0.3
flask-cors==6.0.5
flask-socketio==5.3.6
simple-websocket==1.0.0
psutil==5.9.8
pyautogui==0.9.54
GPUtil==1.4.0

# Screen peek grabs and compresses the desktop; pyautogui pulls this in anyway.
pillow==12.0.0

# Windows-only master volume control
pycaw==20240210; sys_platform == "win32"
comtypes==1.4.16; sys_platform == "win32"
pywin32==312; sys_platform == "win32"
winrt-Windows.Media.Control==3.2.1; sys_platform == "win32"
# The media-control projection uses WinRT date/time values from Foundation.
# It is distributed separately and is not pulled in transitively.
winrt-Windows.Foundation==3.2.1; sys_platform == "win32"
